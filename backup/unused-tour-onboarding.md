## Backup Notes

- `src/components/Onboarding/**` and `src/components/Tour/**` are kept in source as backup and are excluded from `tsconfig` compilation.
- Public exports for Onboarding/Tour are disabled in `src/index.ts`.
- `useAppOnboarding` from the main frontend app was archived as `backup/useAppOnboarding.ts.bak`.
